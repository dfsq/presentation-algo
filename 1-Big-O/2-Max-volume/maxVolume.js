/**
 * Given n non-negative integers a1, a2, ..., an , where each represents a point at coordinate (i, ai).
 * n vertical lines are drawn such that the two endpoints of line i is at (i, ai) and (i, 0).
 * Find two lines, which together with x-axis forms a rectangle container,
 * such that the container contains the max volume.
 *
 *    |
 *    | |     |
 *    |▪|▪ ▪|▪|▪ ▪|
 *    |▪|▪|▪|▪|▪ ▪| |
 *  | |▪|▪|▪|▪|▪ ▪| |
 * [1,5,4,2,3,4,0,3,2] => largest volume is between indeces 1 and 7, => 18
 */
function maxVolume (heights) {
  
}

module.exports = maxVolume
